<h1>Books</h1>

<% @books.each do |list| %>
<h2>Title</h2><h2>Body</h2>
<span><%= book.title %></span>
<% end %>

<h3>New book</h3>
<%= form_with model: @blog, local:true do |f| %>

<h4>Title</h4>
<%= f.text_field :title %>

<h4>Body</h4>
<%= f.text_field :body %>

<%= f.submit 'Create Book' %>

<% end %>